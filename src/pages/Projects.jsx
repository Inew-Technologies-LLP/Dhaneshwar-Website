import Layout from "../components/Layout";

const Projects = () => {
    return (
        <Layout>
            <section className="min-h-screen" />
        </Layout>
    );
};

export default Projects;